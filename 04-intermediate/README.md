# 04 — Intermediate

Reuse and structure.

## Topics
- `@define` with parameters (`@use`)
- `@arr` lists and indexes
- `@import` selective vs `(*)`
- `rpt()` and simple loops

## Goal
Build a reusable helper and import it into a page.
