# 06 — Expert

Composition and performance.

## Topics
- Module design (namespace, tokens, selectors)
- `fscss:name` imports (1.2.1+)
- `runtime.js` vs `esm.js` + `reboot()`
- CLI production output (no runtime)

## Goal
Ship a tiny module others can `@import` cleanly.
