# 05 — Advanced

Logic and dynamic values.

## Topics
- `@event` conditionals
- Arrays + `count` / parallel indexes
- `random()`, `num()`
- Token roots (`*-root`) for themes

## Goal
Drive styles from data (e.g. progress value → color or width).
